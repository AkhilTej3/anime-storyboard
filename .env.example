# =========================
# Core app settings
# =========================
NODE_ENV=development
PORT=5000
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME

# =========================
# Image provider selection
# openai (default) | bedrock
# =========================
IMAGE_PROVIDER=openai

# ==========================================================
# OpenAI / Replit image integration (used when IMAGE_PROVIDER=openai)
# ==========================================================
# Required for current image generation implementation
AI_INTEGRATIONS_OPENAI_API_KEY=your-openai-api-key
# Optional, only if your deployment requires a custom base URL
# AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# ==========================================================
# AWS Bedrock image integration (used when IMAGE_PROVIDER=bedrock)
# ==========================================================
AWS_REGION=us-east-1
# Optional override; defaults to amazon.nova-canvas-v1:0
BEDROCK_MODEL_ID=amazon.nova-canvas-v1:0

# Choose ONE auth method for Bedrock:

# (A) Bedrock API key mode (if your AWS setup provides an API key)
BEDROCK_API_KEY=your-bedrock-api-key

# (B) AWS IAM credentials mode (SigV4)
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_SESSION_TOKEN=your-aws-session-token
